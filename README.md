# Escape room

## Dev setup

You need to install pyenv to setup the dev envionment. After doing so, run:
```
$ pyenv install
$ python -m venv .venv
$ source .venv/bin/activate
$ pip install -r requirements-dev.txt
$ pre-commit install
```